// 前后端共享的常量

// API 端点
export const API_ENDPOINTS = {
  QUERY: '/api/query',
  IMPORT: '/api/import',
  KLINE: '/api/kline'
}

// 默认配置
export const DEFAULT_CONFIG = {
  API_PORT: 8887,
  API_BASE_URL: 'http://localhost:8887'
}

